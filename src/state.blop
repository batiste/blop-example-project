import create from './lib/state.blop'

state: object = {
  todoList: [
    'some task 1',
    'some task 2'
  ],
  page: 'index'
}

def createState(initialState) {
  return create(initialState || state)
}
